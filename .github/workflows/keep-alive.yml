name: Keep App Awake

on:
  schedule:
    - cron: "*/10 * * * *"
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping the app
        run: curl -s -o /dev/null -w "%{http_code}" https://multilingual-app-desk-chatbot-t56dw8u7y6st4dtxfgrkxv.streamlit.app/
